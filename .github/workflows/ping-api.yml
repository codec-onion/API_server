name: Keep API Alive

on:
  schedule:
    - cron: "*/10 * * * *" # Toutes les 10 minutes

jobs:
  ping-api:
    runs-on: ubuntu-latest

    steps:
      - name: Ping API
        run: curl -s -o /dev/null -w "%{http_code}" https://api-server-krgm.onrender.com/ping
